# Specs Index

`specs/` 只保留当前仍然有效、会继续指导实现的文档。

当前有效文档：

- `20260510-220500-current-docs-direction.md`

使用规则：

1. 先看当前有效 spec，再看代码现状。
2. 已归档文档只作为历史决策记录，不再作为当前实现依据。
3. 如果当前实现方向发生变化，应更新当前有效 spec，而不是继续追加平行 spec。
